[build]
  publish = "."
  command = "echo 'Frontend estático listo'"

[dev]
  command = "npx serve ."
  port = 8888

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
